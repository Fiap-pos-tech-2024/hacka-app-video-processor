services:
  app:
    build: .
    volumes:
      - ./video:/app/video
      - ./outputs:/app/outputs
      - ./tmp:/app/tmp
      # Montar credenciais AWS do host
      - ~/.aws:/root/.aws:ro
    environment:
      - NODE_ENV=production

